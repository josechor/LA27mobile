import 'package:flutter/material.dart';

class TweetDetailScreen extends StatelessWidget {
  const TweetDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Tweet Detail Screen');
  }
}
